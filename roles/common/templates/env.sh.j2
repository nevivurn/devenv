#!/usr/bin/env bash

DEVENV_ROOT='{{ devenv_root }}'
DEVENV_ENVS="${DEVENV_ROOT}/envs"

DEVENV_DEBUG="${DEVENV_DEBUG:-}"

for env in "${DEVENV_ENVS}/"*; do
	if [[ ! -f "${env}" ]]; then
		continue
	fi
	if [[ -n "${DEVENV_DEBUG}" ]]; then
		echo "[devenv] sourcing ${env}"
	fi
	source "${env}"
done
